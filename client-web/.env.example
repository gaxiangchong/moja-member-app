# API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:3153

# Shop tab: in-app catalog & checkout (mock data). Uncomment if you restore external shop + SSO handoff to moja-sites.
# VITE_SHOP_WEB_URL=http://localhost:3000/shop
# VITE_SHOP_OPEN_IN_NEW_TAB=true
VITE_FEATURE_SHOP_SSO=false
# Optional: used when generating card payment tokens on the client side
VITE_XENDIT_PUBLIC_KEY=
